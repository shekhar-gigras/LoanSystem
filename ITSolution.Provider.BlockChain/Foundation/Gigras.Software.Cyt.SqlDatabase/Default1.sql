CREATE TABLE [dbo].[IT_Admin] (
    [Id] [int] IDENTITY(1,1) NOT NULL,
        NOT NULULL,
       [datetime] NULme] NULL,
    [     NULL,
ime] NULL,
    CONSTRAINT [PK_IT_Admin] PRIMARY KEY CLUSTERED (
        [Id] ASC
    ) WITH (
        PAD_TATISTICS_NORECOMPUTE = OFF, 
        IGNORE_DUP_KEY = OFF, 
        ALLOW_ROW_LOCKS = ON, 
        ALLOW_PAGE_LOCKS = ON
    ) ON [PRIMARY]
) ON [PRIMARY];
GO